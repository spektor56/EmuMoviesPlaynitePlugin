<UserControl x:Class="EmuMoviesMetadata.EmuMoviesMetadataSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmuMoviesMetadata"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Margin="20">
        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
            <Label VerticalAlignment="Center" Content="Username" HorizontalAlignment="Left" />
            <TextBox MinWidth="100" Name="txtUsername" Margin="10,0,0,0" Text="{Binding Username}"  />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
            <Label VerticalAlignment="Center" Content="Password" HorizontalAlignment="Left" />
            <TextBox MinWidth="100" Name="txtPassword" Margin="10,0,0,0"  Text="{Binding Password}"  />
        </StackPanel>
    </StackPanel>
</UserControl>